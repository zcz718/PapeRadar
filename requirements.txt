# ── Required (core search + note generation) ─────────────────────────────
PyYAML>=6.0
requests>=2.28.0
PyMuPDF>=1.23.0          # PDF text extraction in fetch_fulltext.py

# ── Optional: paper-access fallbacks (fetch_fulltext.py) ─────────────────
# Without these the fetch chain still runs, but it cannot get past
# Cloudflare-fronted / TLS-fingerprinted publishers — the common
# "can't download the PDF even with the VPN on" failure. Install them
# (and, for Playwright, the browser binary) to enable the full chain:
#     pip install curl_cffi playwright pypdf
#     playwright install chromium
curl_cffi>=0.7          # TLS impersonation for Cloudflare hosts (bioRxiv, …)
playwright>=1.40        # last-resort headless-browser Cloudflare bypass
pypdf>=4.0              # secondary PDF text extractor (after PyMuPDF)

# ── Optional: Zotero metadata sync (save_to_zotero.py) ───────────────────
pyzotero>=1.5
